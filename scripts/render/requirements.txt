
# Core AI dependencies
torch>=2.1.0; sys_platform == 'win32'
diffusers>=0.25.0
transformers>=4.36.0
accelerate>=0.26.0

# Video Model specific
# wan-video  # Manually clone repo if needed: https://github.com/DeepBeepMeep/Wan2GP
# Fallback/alternative
modelscope

# Optimization
bitsandbytes>=0.41.0  # For 8-bit quantization

# Utilities
numpy
pillow
tqdm
